# Versioned API used for authentication, sessions, and the report catalog.
VITE_API_BASE_URL=http://localhost:3000/api/v1
# Legacy-compatible survey reporting routes.
VITE_COMPATIBILITY_API_BASE_URL=http://localhost:3000
# Administrative preview exchange routes.
VITE_API_V1_BASE_URL=http://localhost:3000/api
VITE_ADMIN_APP_URL=https://admin.feedbackdatadashboard.com/admin/projects
# Stripe publishable key (the matching secret key remains API-only).
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# When enabled, the login button only accepts the configured test username.
VITE_TEST_USERNAME_ENABLED=false
VITE_TEST_USERNAME=
