# New API. Keep this relative in production when the UI and API share a host.
VITE_API_BASE_URL=/v1

# Local fixtures allow frontend development without staging or production data.
VITE_USE_API_FIXTURES=true
